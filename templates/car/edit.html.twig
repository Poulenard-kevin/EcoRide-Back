{% extends 'base.html.twig' %}

{% block title %}Modifier une voiture{% endblock %}

{% block body %}
    <h1 class="mb-4">Modifier une voiture</h1>

    <div class="card p-4 shadow-sm">
        {{ include('car/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>

    <a href="{{ path('app_car_index') }}" class="btn btn-secondary mt-3 me-2">Retour à la liste</a>

    {{ include('car/_delete_form.html.twig') }}
{% endblock %}