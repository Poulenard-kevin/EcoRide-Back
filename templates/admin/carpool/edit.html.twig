{% extends 'base.html.twig' %}

{% block title %}Modifier un covoiturage{% endblock %}

{% block body %}
    <h1 class="mb-4">Modifier un covoiturage</h1>

    <div class="card p-4 shadow-sm mb-3">
        {{ include('carpool/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>

    <div class="d-flex gap-2">
        <a href="{{ path('app_carpool_index') }}" class="btn btn-secondary">Retour à la liste</a>
        {{ include('carpool/_delete_form.html.twig') }}
    </div>
    
{% endblock %}