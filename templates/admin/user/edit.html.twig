{% extends 'base.html.twig' %}

{% block title %}Modifier un utilisateur{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="mb-4"><i class="bi bi-pencil"></i> Modifier l'utilisateur : {{ user.email }}</h1>

    <div class="card p-4 shadow-sm">
        {{ include('admin/user/_form.html.twig', { 'form': form, 'button_label': 'Modifier' }) }}
    </div>

    <a href="{{ path('app_admin_user_index') }}" class="btn btn-secondary mt-3">
        <i class="bi bi-arrow-left"></i> Retour à la liste
    </a>
</div>
{% endblock %}