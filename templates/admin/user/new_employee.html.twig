{% extends 'base.html.twig' %}

{% block title %}Créer un employé{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="mb-4"><i class="bi bi-person-badge"></i> Créer un nouvel employé</h1>

    <div class="card p-4 shadow-sm">
        {{ include('admin/user/_form.html.twig', { 'form': form, 'button_label': 'Créer' }) }}
    </div>

    <a href="{{ path('app_admin_user_index') }}" class="btn btn-secondary mt-3">
        <i class="bi bi-arrow-left"></i> Retour à la liste
    </a>
</div>
{% endblock %}